import React from 'react';

function Header() {
    return (
        <div id="Header">
            <div className="app-title">
                TodoList
            </div>
        </div>

    );
};

export default Header;